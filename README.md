# pySMFRealTIME - code to get data from SMF memory stream into Python

SMF can write data to SYS1.MAXx datasets, or Logstream  (datasets) and can be configured
to send data directly to an application.
